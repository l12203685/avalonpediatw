# ============================================
# Avalon Pedia - Environment Variables Template
# ============================================

# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
PORT=3001
LOG_LEVEL=debug

# ============================================
# Firebase Configuration
# ============================================
# Get these from Firebase Console > Project Settings

FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_STORAGE_BUCKET=your_bucket.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789
FIREBASE_APP_ID=1:123456789:web:abcdef123456

# Firebase Service Account (for admin SDK)
# Download from Firebase Console > Project Settings > Service Accounts
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your_project.iam.gserviceaccount.com

# ============================================
# Discord Bot Configuration
# ============================================
# Create app at https://discord.com/developers/applications

DISCORD_BOT_TOKEN=xoxb_your_token_here
DISCORD_CLIENT_ID=123456789
DISCORD_GUILD_ID=987654321  # Optional, for testing/development

# Webhook for user feedback/bug reports (posts to your private channel).
# Server `POST /api/feedback` → fan-out to this URL if set.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_id/your_token

# ============================================
# Line Bot Configuration
# ============================================
# Create channel at https://developers.line.biz/

LINE_BOT_CHANNEL_ACCESS_TOKEN=your_line_access_token
LINE_BOT_CHANNEL_SECRET=your_line_channel_secret
LINE_GROUP_ID=your_group_id  # Optional
LINE_USER_ID=your_user_id    # Optional

# LINE Notify OAuth (for push notifications via LINE Notify API)
# Register at https://notify-bot.line.me/my/
LINE_NOTIFY_CLIENT_ID=your_line_notify_client_id
LINE_NOTIFY_CLIENT_SECRET=your_line_notify_client_secret

# ============================================
# Game Configuration
# ============================================
MAX_PLAYERS=10
MIN_PLAYERS=5
VOTE_TIMEOUT=30000        # 30 seconds
QUEST_TIMEOUT=60000       # 1 minute
DISCUSSION_TIMEOUT=120000 # 2 minutes

# ============================================
# Frontend Configuration
# ============================================
VITE_API_URL=http://localhost:3001
REACT_APP_ENV=development

# ============================================
# CORS & Security
# ============================================
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,https://your-domain.com
ALLOWED_DOMAINS=localhost:5173,localhost:3000,your-domain.com

# ============================================
# Session Configuration
# ============================================
SESSION_SECRET=your_random_secret_key_here_generate_with_node
SESSION_EXPIRY=86400000  # 24 hours in milliseconds

# ============================================
# Guest Auth (Plan v2 R1.0 / S10 fix)
# ============================================
# 廢棄舊 client-minted guest uid 的 grace period 截止時間點（ISO 8601）。
# 在這時間之前，舊格式 `JSON.stringify({uid, displayName})` 仍被接受（相容
# 現有玩家快取 token），之後一律要求 client 先 POST /auth/guest 取 server-
# signed JWT。未設定則預設為 server 啟動時間 + 72h。
# 範例：GUEST_LEGACY_CUTOFF=2026-04-23T23:10:00+08:00
# GUEST_LEGACY_CUTOFF=

# ============================================
# Database (Optional)
# ============================================
# DATABASE_URL=postgresql://user:password@localhost:5432/avalon
# REDIS_URL=redis://localhost:6379

# ============================================
# Logging & Monitoring
# ============================================
LOG_LEVEL=info           # debug, info, warn, error
LOG_FORMAT=json          # json, text
SENTRY_DSN=             # Error tracking (optional)

# ============================================
# Email Configuration (Optional)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# ADMIN_EMAIL=admin@your-domain.com

# ============================================
# Phase A — New Login (account + password + email)
# ============================================
# Gmail SMTP 寄信（重設密碼 / 信箱驗證）— 走 avalonpediatw@gmail.com。
#
# GMAIL_SMTP_USER:  寄件人帳號（預設 avalonpediatw@gmail.com）
# GMAIL_APP_PASSWORD: 16 字元 Gmail App Password（到
#   https://myaccount.google.com/apppasswords 產生，避開 2FA）
# MAIL_FROM:        顯示在信件 From 的 email（可與 SMTP_USER 相同）
# FRONTEND_URL:     重設密碼連結用的前端基底 URL（上面 VITE_API_URL 的姊妹）
#
# 沒設定 GMAIL_APP_PASSWORD 時，寄信功能靜默停用（forgot-password 仍回 202，
# 但不會實際寄出）。
# GMAIL_SMTP_USER=avalonpediatw@gmail.com
# GMAIL_APP_PASSWORD=
# MAIL_FROM=avalonpediatw@gmail.com
# FRONTEND_URL=http://localhost:5173
