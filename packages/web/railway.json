{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd ../.. && pnpm install && pnpm -C packages/web build"
  },
  "deploy": {
    "startCommand": "pnpm -C packages/web preview",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowMs": 60000
  }
}
